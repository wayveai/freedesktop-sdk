kind: stack
description: >-
  bst1 script element won't support having regular and junctioned
  element with same name staged under different locations so we
  need a stack as a workaround

depends:
- abi/freedesktop-sdk.bst:abi/desktop-abi-image.bst
