# Definition of directories and paths for the build

builddir: bst_build_dir
sbindir: "%{bindir}"
sysconfdir: "/etc"
localstatedir: "/var"
lib: "lib/%{gcc_triplet}"
indep-libdir: "%{prefix}/lib"
debugdir: "%{indep-libdir}/debug"
ca_path: "%{sysconfdir}/ssl/certs/ca-certificates.crt"
