diff --git a/Makefile b/Makefile
index 14e4283972..7e7eae2464 100644
--- a/Makefile
+++ b/Makefile
@@ -390,7 +390,8 @@ MANPAGES ?= $(MANPAGES_MD:%.md=%)
 MANPAGES_DEST ?= $(subst markdown,man, $(subst source,build,$(MANPAGES)))
 
 $(MANPAGES): %: %.md .install.md2man docdir
-	@sed -e 's/\((podman.*\.md)\)//' -e 's/\[\(podman.*\)\]/\1/' -e 's;<\(/\)\?\(a[^>]*\|sup\)>;;g' $<  | $(GOMD2MAN) -in /dev/stdin -out $(subst source/markdown,build/man,$@)
+	@sed -e 's/\((podman.*\.md)\)//' -e 's/\[\(podman.*\)\]/\1/' -e 's;<\(/\)\?\(a[^>]*\|sup\)>;;g' $<  >$<.tmp
+	@$(GOMD2MAN) -in $<.tmp -out $(subst source/markdown,build/man,$@)
 
 .PHONY: docdir
 docdir:
